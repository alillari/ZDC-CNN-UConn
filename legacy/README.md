# Historical prototypes

These notebooks and scripts are retained for provenance only. They include the
old vertex workflow and a single-seed Lambda-momentum CNN/Mamba comparison.
They are not supported, are not used by the `zdc` CLI, and must not be used as
a physics performance baseline.
